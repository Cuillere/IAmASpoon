var Force = function Force(x, y) {
    this.x = x;
    this.y = y;
};

Force.prototype = {
    add: function(force) {
        this.x += force.x;
        this.y += force.y;
    }
}

module.exports = Force;