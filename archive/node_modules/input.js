exports.handle = function(data, game, sockets){
    var words = data.split(' ');
    if(words[0] == 'pause') {
        game.pause();
        sockets.emit('pause', {});
    }
    if(words[0] == 'resume') {
        game.resume();
        sockets.emit('resume', {});
    }
};